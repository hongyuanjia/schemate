% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-infer.R
\name{schema_infer}
\alias{schema_infer}
\title{Infer a conservative schema from example data}
\usage{
schema_infer(x, version = NULL, keys = c("none", "named", "required", "exact"))
}
\arguments{
\item{x}{Example data to infer from.}

\item{version}{Optional schema document version string.}

\item{keys}{Strategy for inferring optional \code{keys} rules from observed names.
Use \code{"none"} to skip names-rule inference, \code{"named"} to require named
inputs, \code{"required"} to require the observed names to be present, or
\code{"exact"} to require the observed names in the observed order.}
}
\value{
A \code{SchemaDoc} inferred from \code{x}.
}
\description{
\code{schema_infer()} builds a \code{SchemaDoc} from example data using conservative,
structural inference only. It infers base/container check kinds and nested
field structure, but does not guess higher-level authoring constructs such as
\verb{$defs}, \verb{$ref}, \code{keys}, \code{groups}, or combinators.
}
\details{
To parse an existing schema DSL document, use \code{schema_doc()} or
\code{schema_read()} instead.
}
