% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-flat.R
\name{schema_compile}
\alias{schema_compile}
\title{Compile a schema for repeated validation}
\usage{
schema_compile(x)
}
\arguments{
\item{x}{A schema document, raw schema DSL list, \code{SchemaFlat}, or compiled
flat schema node.}
}
\value{
A compiled \code{SchemaFlat}.
}
\description{
\code{schema_compile()} compiles a schema document, raw schema DSL list, or flat
runtime schema node into a \code{SchemaFlat}. Reuse the compiled schema when
validating many inputs against the same schema.
}
\examples{
schema <- schema_doc(list(
    check = list(kind = "list"),
    fields = list(id = list(check = list(kind = "int", lower = 1)))
))

compiled <- schema_compile(schema)
schema_validate(compiled, list(id = 1L), mode = "test")

}
