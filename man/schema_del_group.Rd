% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-edit.R
\name{schema_del_group}
\alias{schema_del_group}
\title{Delete a schema group from a container node}
\usage{
schema_del_group(x, index, path = "$", missing = "error")
}
\arguments{
\item{x}{A \code{SchemaDoc}.}

\item{index}{1-based group index to remove.}

\item{path}{Path to the target container node. Use \code{$} for the root node.
Bare field segments such as \verb{$id} implicitly traverse container \code{fields}. Use
\verb{$fields$id} to write the explicit field path. Backtick-quote field names
that contain path operators, for example \verb{$`a$b` }.}

\item{missing}{Missing-target behavior. Use \code{"error"} to raise an error or
\code{"ignore"} to leave the schema unchanged.}
}
\value{
A modified \code{SchemaDoc}.
}
\description{
Delete a schema group from a container node
}
\examples{
schema <- schema_doc(list(
    check = list(kind = "list"),
    groups = list(schema_group(c("x", "y"), schema_check("number")))
))
schema

schema_del_group(schema, 1)

}
