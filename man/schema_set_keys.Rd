% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-edit.R
\name{schema_set_keys}
\alias{schema_set_keys}
\title{Set a schema node keys rule}
\usage{
schema_set_keys(x, path = "$", ...)
}
\arguments{
\item{x}{A \code{SchemaDoc}.}

\item{path}{Path to the target schema node. Use \code{$} for the root node. Bare
field segments such as \verb{$id} implicitly traverse container \code{fields}. Use
\verb{$fields$id} to write the explicit field path. Backtick-quote field names
that contain path operators, for example \verb{$`a$b` }.}

\item{...}{Named \code{keys} rule arguments passed through to the schema DSL.}
}
\value{
A modified \code{SchemaDoc}.
}
\description{
Set a schema node keys rule
}
\examples{
schema <- schema_doc(list(check = list(kind = "list")))
schema
schema_validate(schema, list(id = 1L), mode = "test")

schema <- schema_set_keys(schema, type = "named", must.include = "id")
schema
schema_validate(schema, list(id = 1L), mode = "assert")

}
