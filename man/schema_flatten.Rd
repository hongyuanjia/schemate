% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema-flat.R
\name{schema_flatten}
\alias{schema_flatten}
\title{Flatten a schema for repeated validation}
\usage{
schema_flatten(x)
}
\arguments{
\item{x}{A schema document, raw schema DSL list, \code{SchemaFlat}, or flattened
flat schema node.}
}
\value{
A flattened \code{SchemaFlat}.
}
\description{
\code{schema_flatten()} converts a schema document, raw schema DSL list, or flat
runtime schema node into a \code{SchemaFlat}. Reuse the flattened schema when
validating many inputs against the same schema.
}
\examples{
schema <- schema_doc(list(
    check = list(kind = "list"),
    fields = list(id = list(check = list(kind = "int", lower = 1)))
))

flat <- schema_flatten(schema)
schema_validate(flat, list(id = 1L), mode = "test")

}
