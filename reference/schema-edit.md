# Schema edit verbs

Edit verbs return modified copies of `SchemaDoc` objects. Paths use `$`
for the root node and bare field segments such as `$id` for container
fields.

## Details

Backtick-quote field names that contain path operators. Use `$fields$id`
when you want the explicit field path.
