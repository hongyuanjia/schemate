#' schemate: Checkmate-first schema inference, editing, JSON IO, and validation
#'
#' `schemate` provides a compact schema DSL for R data. It can infer schemas
#' from example objects, edit schema documents, read and write schema JSON, and
#' validate inputs against compiled schemas.
#'
#' @keywords internal
"_PACKAGE"
